// Write a program to “fold” long input lines into two or
// more shorter lines after the last non-blank character that occurs before
// the n-th column of input. Make sure your program does something
// intelligent with very long lines,
// and if there are no blanks or tabs before the specified column.
// todo
//
#include <stdio.h>

#define FIXED_SIZE 10

void fold(void);

void getLine(char line[], int size);

int main() {
    return 0;
}

/*
 * read line and fold line
 */
void fold() {
    int c;

    while ((c = getchar()) != EOF) {

    }
}